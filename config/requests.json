{
    "searchConversation": {
        "schema": {
            "method": "GET",
            "host": "<%= iparams.freshchatDomain %>",
            "path": "/v2/conversations/ <%= conversation_id %> /messages?page=1&items_per_page=50",
            "headers": {
                "Authorization": "Bearer <%= encode(iparam.api_key) %>"
            }
        }
    }
}